# OpenRouter CLI Chat

Интерактивный CLI-приложение для общения с LLM через OpenRouter API с поддержкой локальных Model Context Protocol (MCP) серверов.

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![TypeScript](https://img.shields.io/badge/TypeScript-5-blue.svg)
![Node.js](https://img.shields.io/badge/Node.js-18+-green.svg)

## 📋 Описание

**OpenRouter CLI Chat** объединяет мощь больших языковых моделей с локальными инструментами через Model Context Protocol. Приложение позволяет LLM получать контекст из локальных файлов, выполнять пользовательские операции и предоставлять более информативные ответы.

### Основные возможности

- 🤖 **Интеграция с OpenRouter API** - поддержка множества LLM моделей
- 🔧 **MCP Серверы** - подключение локальных инструментов и ресурсов
- 📚 **Работа с документами** - встроенный file-mcp-server для чтения и редактирования локальных файлов
- 💬 **Интерактивный чат** - CLI интерфейс для общения с AI
- 🔄 **Функциональные вызовы** - LLM может автоматически использовать инструменты
- 📝 **MCP Ресурсы** - система документов как ресурсов для будущего использования приложением
- 🛡️ **TypeScript** - полная типизация для надёжности кода

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+
- npm 9+
- API ключ OpenRouter (получить на [openrouter.ai](https://openrouter.ai))

### Установка

1. **Клонируйте репозиторий**

   ```bash
   git clone https://github.com/yourusername/openrouter-cli-chat.git
   cd openrouter-cli-chat
   ```

2. **Установите зависимости**

   ```bash
   npm install
   ```

3. **Настройте окружение**

   ```bash
   cp .env.example .env
   ```

   Отредактируйте `.env` и добавьте ваш OpenRouter API ключ:

   ```
   OPENROUTER_API_KEY=your_api_key_here
   ```

4. **Соберите проект**

   ```bash
   npm run build
   ```

5. **Запустите приложение**

   ```bash
   npm run start
   ```

## 📖 Использование

После запуска приложение будет ждать вашего ввода:

```
You: Расскажи мне о содержимом файла docs/summary.txt
Assistant: [LLM использует file-mcp-server для чтения файла и предоставляет ответ]

You: exit
```

## 🏗️ Архитектура

Проект состоит из трёх основных компонентов:

### 1. MCP Client (`src/mcp-clients/`)

- Управляет циклом взаимодействия LLM-MCP
- Обрабатывает вызовы инструментов
- Ведёт историю разговора

### 2. OpenRouter Client (`src/openrouter/`)

- Обёртка вокруг OpenRouter SDK
- Управляет параметрами модели
- Поддерживает функциональные вызовы

### 3. MCP Servers (`src/mcp-servers/`)

- **file-mcp-server** - работает с локальными документами:
  - Чтение документов через инструмент `get_user_docs`
  - Редактирование документов через инструмент `edit_user_docs`
  - Чтение документов как ресурсов MCP
  - Все документы автоматически загружаются из директории `docs/`
- Легко расширяется новыми инструментами и ресурсами
- Поддерживает registered prompts для специализированных задач

```
┌─────────────┐
│  User CLI   │
└──────┬──────┘
       │
┌──────▼──────────────┐
│   MCPClient         │
│  (orchestrator)     │
└──┬─────────────┬────┘
   │             │
   │             │
┌──▼─────┐  ┌───▼──────┐
│OpenRouter │ │MCP Servers
│Client     │ │(file, ...)
└──────────┘ └──────────┘
```

## 🛠️ Доступные команды

### Разработка

```bash
# Сборка проекта
npm run build

# Запуск приложения
npm run start

# Проверка кода (Linting)
npm run lint

# Автоматическое исправление ошибок
npm run lint:fix

# Форматирование кода
npm run format

# Проверка форматирования
npm run format:check
```

## 🔧 Конфигурация

### Переменные окружения

- `OPENROUTER_API_KEY` - ваш API ключ OpenRouter (обязательно)
- `LLM_MODEL` - выбранная модель (по умолчанию: `mistralai/mistral-7b-instruct`)

### Добавление документов

Поместите файлы документов в `src/mcp-servers/file-mcp-server/docs/`:

```
src/mcp-servers/file-mcp-server/docs/
├── readme.txt
├── guide.md
└── data.json
```

LLM сможет использовать инструмент `get_user_docs` для чтения этих файлов.

## 🔧 Отладка MCP серверов

Для локальной отладки MCP сервера используйте инспектор MCP:

```bash
npx @modelcontextprotocol/inspector node dist/mcp-servers/file-mcp-server/index.js
```

Это откроет интерфейс инспектора, где вы сможете:
- Видеть зарегистрированные инструменты и их описания
- Вызывать инструменты и смотреть результаты
- Проверять доступные ресурсы и prompts
- Отлаживать работу сервера до интеграции с основным приложением

## 📦 Зависимости

Основные пакеты:

- `@modelcontextprotocol/sdk` - MCP протокол
- `@openrouter/sdk` - OpenRouter API
- `zod` - валидация схем
- `dotenv` - управление переменными окружения
- `typescript` - язык разработки
- `eslint` - проверка кода
- `prettier` - форматирование кода

## 📝 Примеры использования

### Получение информации из локальных файлов

```
You: Какие основные пункты в файле guide.md?
Assistant: Проверим содержимое файла guide.md...
[Ответ на основе содержимого файла]
```

### Выполнение нескольких операций

```
You: Прочитай файлы docs.txt и summary.txt, затем помоги мне их объединить
Assistant: Я прочитаю оба файла для вас...
[Использует file-mcp-server дважды и предлагает объединённый результат]
```

## 🤝 Вклад в проект

Мы приветствуем вклады! Для участия:

1. Форкируйте репозиторий
2. Создайте ветку для функции (`git checkout -b feature/amazing-feature`)
3. Коммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Пушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Проект лицензирован под MIT лицензией - см. файл [LICENSE](LICENSE) для деталей.

## 👤 Автор

Проект создан как демонстрация интеграции OpenRouter API с Model Context Protocol.

## 🔗 Полезные ссылки

- [OpenRouter API Документация](https://openrouter.ai/docs)
- [Model Context Protocol](https://modelcontextprotocol.io)
- [Node.js Документация](https://nodejs.org)
- [TypeScript Документация](https://www.typescriptlang.org)

## 💡 Советы и подсказки

- Регулярно обновляйте зависимости для безопасности
- Тестируйте новые MCP серверы локально перед использованием
- Используйте `.env.example` как шаблон для конфигурации
- Обратитесь к [CLAUDE.md](CLAUDE.md) для информации о разработке

## 🐛 Сообщение об ошибках

Если вы обнаружили ошибку, пожалуйста:

1. Проверьте [Issues](https://github.com/yourusername/openrouter-cli-chat/issues)
2. Создайте новый Issue с подробным описанием
3. Включите версию Node.js и OS
4. Приложите логи ошибок

## 📞 Поддержка

Для вопросов и поддержки:

- Откройте Issue на GitHub
- Проверьте существующую документацию в [CLAUDE.md](CLAUDE.md)
- Обратитесь к примерам в репозитории

---

Спасибо за использование OpenRouter CLI Chat! 🙌
